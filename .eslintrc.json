{
  "extends": ["next/core-web-vitals", "next/typescript"],
  "rules": {
    // Disable rules that conflict with our current workarounds
    "@typescript-eslint/no-explicit-any": "off",
    "react-hooks/exhaustive-deps": "off", // Optional: to silence the useEffect dependency warning
    "no-unused-vars": "off" // Optional: to silence the unused pfpUrl variable error
  }
}